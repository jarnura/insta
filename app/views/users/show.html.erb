<%= @user.name %>
<% if User.find(session[:user_id]).follows?(@user) %>
<%= button_to "unsubcribe",unsub_path, class: "btn blue-gradient btn-rounded" %>
<% else %>
<%= button_to "subcribe",sub_path, class: "btn peach-gradient btn-rounded" %>
<% end %>
